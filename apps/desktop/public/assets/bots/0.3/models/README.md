# SUPERIOR 0.3 GLB Models

Runtime model convention:

- `gremlin.glb` is the first flagship sculpt target.
- GLB assets should be glTF 2.0 binary files with baked clay-friendly proportions and no external texture paths.
- The desktop runtime keeps procedural geometry as fallback until a model is explicitly enabled in code.
