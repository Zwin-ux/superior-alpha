# Sample Synergy Feed Reader

This is a tiny JS/TS folder for SUPERIOR custom skill import smoke tests.

Drop this folder onto the Workshop skill tray or paste its path into the custom part field. The daemon should return a proposal only; it should not equip the part until a future adapter smoke run exists.
